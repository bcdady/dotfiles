{{- $bwEmail := output "sh" "-c" "bw status 2>/dev/null | jq -r '.userEmail // empty'" | trim -}}

[user]
{{- if $bwEmail }}
	email = {{ $bwEmail }}
{{- else }}
	# email not set — run 'bw login' then 'chezmoi apply'
{{- end }}
