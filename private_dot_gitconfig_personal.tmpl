{{- $bwStatus := output "sh" "-c" "bw status 2>/dev/null | jq -r .status || echo unauthenticated" | trim -}}

[user]
{{- if eq $bwStatus "unlocked" }}
	email = {{ (bitwarden "item" "github.com").login.username }}
{{- else }}
	# email not set — run 'bw login && bw unlock' then 'chezmoi apply'
{{- end }}
